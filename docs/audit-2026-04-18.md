# Workforce0 Post-Pivot Audit — 2026-04-18

Consolidated output from 10 parallel audit agents run on branch `feat/oss-pivot` (commit `42831bd`), covering: backend routes, backend services, frontend UI, integrations, agent daemon, Prisma schema, test coverage, deployment, authentication, and dev tooling.

> **Pivot recap:** Workforce0 just moved from SaaS (with Stripe billing, multi-tenant, WorkOS SSO as primary) to **open-source, self-hosted**. Target user is unchanged — non-technical executives. Users `git clone`, run `docker compose up`, BYOK for AI keys. Goal: trending GitHub repo (Cal.com / Plausible / n8n / PostHog pattern).

---

## REMOVE

### Code
- `mvp/src/routes/billing.routes.ts` — Stripe checkout/portal/status/webhook handlers (lines 54–200)
- `mvp/src/services/billing/` — full directory
- `mvp/src/services/billing/__tests__/billing.service.test.ts` — 348 lines of Stripe-mock tests
- `mvp/src/services/usage/usage.service.ts` — spending-cap metering tied to billing tiers
- Stripe customer creation in `auth.routes.ts:322–334` (non-fatal but still runs)
- `frontend/src/app/(dashboard)/settings/billing/page.tsx` — full SaaS pricing/trial/tier UI
- `frontend/src/app/(dashboard)/settings/page.tsx:1170–1183` — "Billing & Plans" card
- Dead `meetings.routes.ts` 410 handler (Recall.ai scheduling deprecation)
- Hardcoded `wss://api.workforce0.com` in `agent/src/index.ts:31` (use `WF0_SERVER` env var)
- Fake-disabled Jira stub at `jira.service.ts:227` returning `{id:'disabled', key:'DISABLED-0'}`
- Trial/tier language in `dashboard.routes.ts:153–209` (reframe as OSS metrics)

### Schema (`mvp/prisma/schema.prisma`, `Tenant` model, lines 28–35)
- `tier`
- `trialEndsAt`
- `stripeCustomerId`
- `stripeSubscriptionId`
- `meetingsUsedThisMonth`
- `prdsUsedThisMonth`
- `billingPeriodStart`

### Config
- `STRIPE_PRICE_STARTER` + all Stripe env plumbing in `mvp/src/config/index.ts`

> **Keep:** `mvp/src/services/auth/workos.service.ts` — Agent 9 confirmed SSO is correctly gated (returns 501 without env vars). Remove only if simplifying further; not required for OSS launch.

---

## ADD

### Backend
- `IntegrationConnection` Prisma model — generic BYOK token vault (currently no storage for Jira/Slack/etc. credentials except inside legacy `tenant` columns)
- `AgentToken.rotatedAt` field for key-rotation policy
- Compound indexes: `Meeting`, `AgentTask`, `PRD`, `Notification` on `(workspaceId, createdAt)`
- `mvp/src/services/integrations/wizard.service.ts` — unified wizard backend
- `POST /integrations/:name/connect` — single endpoint for all wizards
- `mvp/src/services/setup/first-run.service.ts` + `POST /setup/wizard` endpoint
- `GET /system/status` — component readiness (DB, Redis, queue, AI providers)
- `testConnection()` methods on `gchat.service.ts`, `gdocs.service.ts`, `github.service.ts`, Twilio
- Slack service (schema has `slackBotToken`/`slackDefaultChannel` fields but no service)
- Linear and Notion services
- Wire `auth.routes.ts:949` password-reset TODO to `CommunicationRouter`
- SMTP backend for `communication/channels/email.channel.ts` (currently SendGrid-only with silent no-op)
- Pluggable storage for `archive/archive.service.ts` (local-disk fallback when S3 absent)

### Frontend (per `docs/stitch-prompts/`)
- `frontend/src/app/(onboarding)/setup/page.tsx` — 4-step first-run wizard (Stitch 01)
- `frontend/src/app/(dashboard)/approvals/page.tsx` — two-pane queue with J/K nav (Stitch 07)
- `frontend/src/app/(dashboard)/settings/integrations/page.tsx` — card grid (Stitch 06)
- Reusable integration wizard modal (Stitch 05) — 3-step: URL → token → test
- In-app agent token generator UI (currently REST-only)
- In-app SSO config toggle (settings)

### Deployment
- `railway.json` — Railway template
- `render.yaml` — Render Blueprint
- `fly.toml` — Fly.io config
- `.do/app.yaml` — DigitalOcean App Platform spec
- `docs/deploy-vercel.md` — Vercel + Supabase + Upstash walkthrough
- `.github/workflows/docker-publish.yml` — GHCR/Docker Hub on release
- `.github/workflows/release.yml` — release automation
- Frontend healthcheck in `docker-compose.prod.yml`
- Assets referenced by README: `docs/assets/logo.svg`, `onboarding.png`, `integrations.png`, `approvals.png`

### Docs
- `mvp/docs/jira-setup.md` (with screenshots)
- `mvp/docs/github-setup.md` (PAT scopes)
- `mvp/docs/slack-setup.md`
- `mvp/docs/linear-setup.md`
- `mvp/docs/notion-setup.md`
- `mvp/docs/google-meet-setup.md`
- `mvp/prisma/migrations/README.md` — backup + upgrade guidance

### Tests
- Playwright E2E: first-run setup wizard, per-integration wizard (Jira/Slack), BYOK key roundtrip, first-user-admin, fresh-install migration, agent WebSocket reconnect
- Coverage thresholds in `mvp/vitest.config.ts` and `frontend/vitest.config.ts`

### OSS tooling
- `frontend/eslint.config.mjs` + `lint` npm script (**CI currently fails without this**)
- `.husky/pre-commit` + `.lintstagedrc.json`
- `.changeset/config.json` + changeset README
- `renovate.json`
- `CODEOWNERS`
- `.editorconfig`

---

## PENDING (half-built)

### Known TODOs / stubs
- `auth.routes.ts:949` — password-reset email (TODO, silent no-op)
- `ai/openai.service.ts:136` — returns mock critique when key missing (should skip cleanly)
- `agents/qa/tools.ts` (89–91, 220, 315, 425), `agents/dev/tools.ts` (81–85, 333, 426, 537), `agents/supervisor/tools.ts:20` — many stubs pending GitHub MCP
- `communication/channels/email.channel.ts:37–40` — silent no-op in prod when SendGrid absent (prod risk)
- `archive/archive.service.ts:54, 95` — disables when S3 absent, no local fallback

### Misaligned with Stitch designs
- `frontend/src/components/onboarding-wizard.tsx` — tool-picker today; Stitch 01 specifies 4-step (workspace name → BYOK → first integration → ready)
- `frontend/src/app/(dashboard)/prds/[id]/page.tsx` — scattered approval logic, should delegate to `/approvals`
- `/settings/models`, `/analytics` — developer-centric, need exec narrative

### Naming / consistency
- Rename `tenantId` → `workspaceId` across all 27 Prisma models (per `AGENTS.md`)

### Agent daemon
- Job types `review_pr` and `run_tests` have prompt templates but no post-execution logic (only `implement_prd` full flow)
- No max-reconnect-attempts documented

### Repo hygiene
- Placeholder URLs in ~10 files: `your-org/workforce0`, `workforce0.com`, `discord.gg/workforce0`
- Two conflicting `.env.example` files (root vs `mvp/`) — `CONTRIBUTING.md` setup instructions broken regardless
- CI references `npm run lint` for frontend — script doesn't exist, CI will fail on fresh checkout

---

## Summary

- **~40** items to REMOVE
- **~50** items to ADD
- **~15** pending items to FINISH

## Suggested execution order

1. Fix CI (frontend lint + placeholder URLs) — unblocks everything
2. Rip out billing (code + schema + frontend + tests + config) — clean slate
3. Add one-click deploy templates + Docker publish workflow — README stops lying
4. Build `IntegrationConnection` model + first wizard (Jira as template) — the single most important non-dev UX moment
5. Build first-run setup wizard — first 5 minutes of every install
6. Add remaining integrations (Slack, Linear, Notion) using Jira wizard as template
7. Polish approval queue page + integrations grid per Stitch designs
8. Fill stubbed services (finish or cleanly disable with user-facing status)
9. Add E2E tests + coverage thresholds
10. Rename `tenantId` → `workspaceId` (last, so churn is minimal)

---

*Generated from 10 parallel audit agents on 2026-04-18. Source transcripts preserved in agent output files.*
